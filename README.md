# Luna

Luna is a small C++ library for building clean and type safe bindings for Luau. It keeps the Luau C API inside the library so application code can use a simpler interface.

The project is still getting started. Right now it provides a basic runtime wrapper and a small test app.

## Building

```bat
cmake --preset ninja-debug
cmake --build --preset ninja-debug
```
